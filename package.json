{
  "type": "module",
  "scripts": {
    "test": "jest",
    "prebuild": "rm -rf dist",
    "build": "./node_modules/.bin/esbuild app.jsx --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=out.js",
    "postbuild": "cd dist && zip -r index.zip index.js*"
  },
  "devDependencies": {
    "@babel/core": "^7.21.4",
    "@babel/preset-env": "^7.21.4",
    "@babel/preset-typescript": "^7.21.4",
    "@types/aws-lambda": "^8.10.114",
    "aws-lambda": "^1.0.7",
    "babel-jest": "^29.5.0",
    "esbuild": "0.17.15",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.3"
  },
  "dependencies": {
    "node-fetch": "^3.3.1",
    "sequelize": "^6.30.0",
    "sequelize-typescript": "^2.1.5"
  }
}
